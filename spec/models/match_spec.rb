require "rails_helper"

RSpec.describe Match, :type => :model do
=begin
  it "return true" do
    first = Match.create!(str_1: "elvis", str_2: "lives", match: true)
    #second = Match.create!(str_1: "abacate", str_2: "etacaba")

    expect(Match['result']).to eq(true)
  end
=end
end
